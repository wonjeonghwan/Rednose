<UserControl x:Class="APR_Rednose.View.CameraView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="Transparent">

    <!-- 로컬에서 바로 쓰는 변환 -->
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Background="Transparent">
        <!-- 평소 카메라 프리뷰 -->
        <Image Source="{Binding Preview}"
               Stretch="Uniform"
               SnapsToDevicePixels="True"
               UseLayoutRounding="True">
            <!-- 프리뷰를 감추고 싶을 때 -->
            <Image.Style>
                <Style TargetType="Image">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasCameraError}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Image.Style>
        </Image>

        <!-- 에러 오버레이: 카메라 미연결 안내 -->
        <Border Background="#CC000000"
                Visibility="{Binding HasCameraError, Converter={StaticResource BoolToVis}}"
                Panel.ZIndex="200">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="카메라를 연결할 수 없습니다."
                           Foreground="White" FontSize="22" FontWeight="Bold"
                           HorizontalAlignment="Center" TextAlignment="Center"/>
                <TextBlock Text="연결 상태를 확인해주세요"
                           Foreground="White" Opacity="0.85" FontSize="16"
                           Margin="0,8,0,0" HorizontalAlignment="Center" TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
